<%= form_with(model: active_hour, local: true) do |form| %>
  <% if active_hour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(active_hour.errors.count, "error") %> prohibited this active_hour from being saved:</h2>

      <ul>
      <% active_hour.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :start %>
    <%= form.datetime_select :start %>
  </div>

  <div class="field">
    <%= form.label :end %>
    <%= form.datetime_select :end %>
  </div>

  <div class="field">
    <%= form.label :tutor_id %>
    <%= form.number_field :tutor_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
