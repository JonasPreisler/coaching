<div class="row statistics">
	<div class="col-md-4 col-sm-4">
		<div class="box">
			<h3>Side visninger</h3>
					
					
			<div class="row page-views">
				<div class="col-md-8 col-sm-8">
					<p>I dag:</p>
					<p>Denne uke:</p>
					<p>Denne måned:</p>
				</div>
				<div class="col-md-4 col-sm-4 stats">
					<%= @tutor.impressionist_count(:filter=>:ip_address, start_date: Date.today, end_date: Time.now) %><br>
					<%= @tutor.impressionist_count(:filter=>:ip_address, start_date: Date.today-7.days, end_date: Time.now) %><br>
					<%= @tutor.impressionist_count(:filter=>:ip_address, start_date: Date.today-1.month, end_date: Time.now) %>
				</div>
			</div>
	 </div>
	</div>
	<div class="col-md-8 col-sm-8">
		<div class="box">

			<%= column_chart @tutors.group_by_month(:created_at, format: "%b %y", range: 1.year.ago..Time.now).count, height: '180px', colors: ["#00a85b"], prefix: 'Rådgivere ' %>

			<%#= @tutor.tutors_categories.count %>
		</div>
	</div>
</div>