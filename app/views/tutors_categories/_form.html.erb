<%= form_with(model: tutors_category, local: true) do |form| %>
  <% if tutors_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tutors_category.errors.count, "error") %> prohibited this tutors_category from being saved:</h2>

      <ul>
      <% tutors_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tutor_id %>
    <%= form.number_field :tutor_id %>
  </div>

  <div class="field">
    <%= form.label :sub_category_id %>
    <%= form.number_field :sub_category_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
