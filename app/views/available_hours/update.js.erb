$('.calendar').fullCalendar('removeAvailableHours', [<%= @available_hour.id %>]);
$('.calendar').fullCalendar(
  'renderAvailableHour', 
  $.parseJSON("<%=j render(@available_hour, format: :json).html_safe %>"), 
  true
);
$('.modal').modal('hide');