<%= link_to 'Sign up', new_account_registration_path unless current_or_guest_account %> - <%= link_to 'Sign in', new_account_session_path unless current_or_guest_account %> - <%= link_to 'Log out', destroy_account_session_path, method: :delete if current_or_guest_account %><br><br>
<%= link_to 'Kategorier', categories_path %>
<hr>

<% @accounts.each do |acc| %>
	<%= acc.email %> <%= '<i>Gjest</i>'.html_safe if acc.guest %><%#= 'Online' if acc.online? %>
	<%#= 'Offline' if !acc.online? %>
	<br><br>
<% end %>